groups:
- name: "service monitoring"
  rules:
  - alert: service_down
    expr: up == 0
    for: 5s
    labels:
      severity: page
    annotations:
      summary: "{{ $labels.job }} is down."
      description: "{{ $labels.job }} is down."